.block
  .secondary-navigation
    %ul.wat-cf
      %li.first= link_to "#{t("web-app-theme.list", :default => "List")}", okrs_path
      %li.active= link_to "#{t("web-app-theme.new", :default => "New")}", new_okr_path
  .content
    %h2.title
      = t("web-app-theme.new", :default => "New")
      Okr
    .inner
      = simple_form_for @okr, :url => okrs_path, :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f}
- content_for :sidebar, render(:partial => 'sidebar')
